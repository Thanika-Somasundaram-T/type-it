const seconds = 5;
const milliSeconds = 1000;

const config = {
	increment: 1,
	decrement: 1,
	bonus: 5,
	tickerDelay: milliSeconds * seconds,
	life: 5,
	decreaseHealth: 10,
};

export default config;
